#include <stdio.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    int n1;
    scanf("%d",&n1);
    int arr1[n1];
    for(int i=0;i<n1;++i){
        scanf("%d",&arr1[i]);
    }
    
    int n2;
    scanf("%d",&n2);
    int arr2[n2];
    for(int i=0;i<n2;++i){
        scanf("%d",&arr2[i]);
    }
    
    int i=0;
    int j=0;
    while(i<n1 && j<n2){
        if(arr1[i]<arr2[j]){
            printf("%d ",arr1[i]);
            i++;
        }
        else if(arr1[i]>arr2[j]){
            printf("%d ",arr2[j]);
            j++;
        }
        else{
            printf("%d ",arr2[j]);
            printf("%d ",arr2[j]);
            i++;
            j++;
        }
    }
    
    while(i<n1){
        printf("%d ",arr1[i]);
        ++i;
    }
    while(j<n2){
        printf("%d ",arr2[j]);
        ++j;
    }
    
    return 0;
    
}#include <stdio.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    int n1;
    scanf("%d",&n1);
    int arr1[n1];
    for(int i=0;i<n1;++i){
        scanf("%d",&arr1[i]);
    }
    
    int n2;
    scanf("%d",&n2);
    int arr2[n2];
    for(int i=0;i<n2;++i){
        scanf("%d",&arr2[i]);
    }
    
    int i=0;
    int j=0;
    while(i<n1 && j<n2){
        if(arr1[i]<arr2[j]){
            printf("%d ",arr1[i]);
            i++;
        }
        else if(arr1[i]>arr2[j]){
            printf("%d ",arr2[j]);
            j++;
        }
        else{
            printf("%d ",arr2[j]);
            printf("%d ",arr2[j]);
            i++;
            j++;
        }
    }
    
    while(i<n1){
        printf("%d ",arr1[i]);
        ++i;
    }
    while(j<n2){
        printf("%d ",arr2[j]);
        ++j;
    }
    
    return 0;
    
}
