#include <stdio.h>

int find_unique_digit_product(int n) {
    // Implement the function
}

int main() {
    int n;
    scanf("%d", &n);
    printf("%d\n", find_unique_digit_product(n));
    return 0;
}



